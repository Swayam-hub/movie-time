import { UploadForm } from "@/components/form/UploadForm";
import React from "react";

const Upload = () => {
  return (
    <div>
      <h1 className="font-semibold text-4xl text-center">Upload Movie</h1>
      <UploadForm />
    </div>
  );
};

export default Upload;
